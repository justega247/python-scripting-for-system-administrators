
# Utilize pip to install the psycopg2 library (a PostgreSQL database library). Be sure to first create and activate
# a virtualenv before installing the package.

$ mkdir venvs
$ python3.6 -m venv venvs/pg
$ source venvs/pg/bin/activate
(pg) $ pip install psycopg2

